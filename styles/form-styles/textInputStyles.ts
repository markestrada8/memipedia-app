import { StyleSheet } from 'react-native'
import { colors } from '../colors'

export default StyleSheet.create({
  textInput: {
    backgroundColor: '#fff',
    borderRadius: 10,
    height: 40,
    paddingLeft: 20
  },
  textInputContainer: {
    marginTop: 20,
    marginBottom: 20
  },
  textElement: {
    color: '#fff',
    fontSize: 20,
    fontWeight: '700'
  }
})